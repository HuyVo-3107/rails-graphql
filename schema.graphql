type Comment {
  body: String
  createdAt: ISO8601DateTime!
  id: ID!
  postId: Int!
  updatedAt: ISO8601DateTime!
}

"""
Autogenerated input type of CreatePost
"""
input CreatePostInput {
  """
  A unique identifier for the client performing the mutation.
  """
  clientMutationId: String
  postInput: PostInput!
}

"""
An ISO 8601-encoded datetime
"""
scalar ISO8601DateTime

type Mutation {
  createPost(
    """
    Parameters for CreatePost
    """
    input: CreatePostInput!
  ): Post
}

type Post {
  comments(cursor: Int): [Comment!]
  createdAt: ISO8601DateTime!
  id: ID
  status: PostStatus
  title: String
  updatedAt: ISO8601DateTime!
}

input PostInput {
  title: String!
}

"""
Post status enum
"""
enum PostStatus {
  ACTIVE
  CANCEL
}

type Query {
  comments: [Comment!]!

  """
  Fetch list posts
  """
  posts: [Post!]!
  postsWithFilter(first: Int, title: String): [Post!]!
}
